# CGV-Plugin to generate plots in the fragment shader

### Compile
```commandline
mkdir build
cd build
cmake ..
cmake --build . -j
cd ..
```
### Run
#### Linux
```commandline
chmod +x RUNME.sh
./RUNME.sh
```
#### Windows
```commandline
.\RUNME.bat
```